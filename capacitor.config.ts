import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'figura_publica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
